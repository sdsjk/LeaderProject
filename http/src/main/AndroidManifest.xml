<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.linewell.http">

    <application android:allowBackup="true" android:label="@string/app_name"
        android:supportsRtl="true">

        <meta-data android:name="APP_ID"
            android:value="${APP_ID}">
            </meta-data>
        <meta-data android:name="SITE_ID"
            android:value="${SITE_ID}">
        </meta-data>
        <meta-data android:name="SITE_AREA_CODE"
            android:value="${SITE_AREA_CODE}">
        </meta-data>

    </application>

</manifest>
